# Direct runtime dependencies for GLIDE.
#
# Install a CUDA-compatible PyTorch build first:
# https://pytorch.org/get-started/locally/
#
# PyTorch Geometric must match the installed PyTorch/CUDA version. If the
# generic pip package fails, follow the official wheel selector:
# https://pytorch-geometric.readthedocs.io/en/latest/install/installation.html

numpy>=1.21,<2.0
einops>=0.6.0
tqdm>=4.64.0

matplotlib>=3.5.0
seaborn>=0.11.0
tensorboard>=2.11.0

setproctitle>=1.3.0
torch-ema>=0.3
torch-geometric>=2.0.0
